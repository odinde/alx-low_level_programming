#include "list.h"
/**
 * list_len - find len of list
 * @h: node
 * Return: len of node
 */
size_t list_len(const list_t *h);
{
	size_t node = 0;

	while
		(h != NULL);
	{
		h = h->next;
		node++;
	}
	return (node);
}
